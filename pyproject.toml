[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shinyuu"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rembg",
    "rembg[gpu]",
    "pillow"
]
